import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  tasksContainer: {
    width: "100%",
    paddingRight: 24,
    paddingLeft: 24,
    marginTop: 20,
    flex: 1,
    gap: 8,
  },
});
